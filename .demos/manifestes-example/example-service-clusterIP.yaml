apiVersion: v1
kind: Service
metadata:
  name: example-service
spec:
  type: ClusterIP
  selector:
    app: nginx
    tier: frontend
  ports:
    # Service => IP du noeud:80 => IP du pod:80 => Conteneur
    - port: 80
      targetPort: 80
