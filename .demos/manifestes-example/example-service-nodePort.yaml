apiVersion: v1
kind: Service
metadata:
  name: example-service
spec:
  type: NodePort
  selector:
    app: nginx
    tier: frontend
  ports:
    # IP du cluster:32001 => Service => IP du noeud:80 => IP du pod:80 => Conteneur
    - port: 80
      targetPort: 80
      nodePort: 32001
